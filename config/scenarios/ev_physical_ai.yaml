theme: ev_physical_ai
_generated_by: config/scenarios/generate.py (dz符号ベースの機械生成、統計未実証の暫定シナリオ)
scenarios:
  bull:
    conditions:
    - id: us10y_yield_dz_bull
      desc: us10y_yield の勢い(21日変化率Zスコア)が>0.0
      indicator: us10y_yield
      feature: dz
      op: '>'
      threshold: 0.0
      weight: 0.5
  neutral:
    conditions:
    - id: us10y_yield_dz_neutral
      desc: us10y_yield の勢い(21日変化率Zスコア)がabs_lt0.5
      indicator: us10y_yield
      feature: dz
      op: abs_lt
      threshold: 0.5
      weight: 0.5
  bear:
    conditions:
    - id: us10y_yield_dz_bear
      desc: us10y_yield の勢い(21日変化率Zスコア)が<0.0
      indicator: us10y_yield
      feature: dz
      op: <
      threshold: 0.0
      weight: 0.5
    - id: risk_regulation_bear
      desc: Layer6リスクカテゴリ「regulation」のスコアが60以上(悪化)
      indicator: risk:regulation
      feature: level
      op: '>='
      threshold: 60.0
      weight: 0.3
    - id: risk_dilution_bear
      desc: Layer6リスクカテゴリ「dilution」のスコアが60以上(悪化)
      indicator: risk:dilution
      feature: level
      op: '>='
      threshold: 60.0
      weight: 0.3
    - id: risk_customer_churn_bear
      desc: Layer6リスクカテゴリ「customer_churn」のスコアが60以上(悪化)
      indicator: risk:customer_churn
      feature: level
      op: '>='
      threshold: 60.0
      weight: 0.3
