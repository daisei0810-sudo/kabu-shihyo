# このファイルをコピーして .env (gitignore対象) を作り、実際のキーを記入する。
# python -m src.main が起動時に自動読み込みする(既存のOS環境変数は上書きしない)。

FRED_API_KEY=
COINGECKO_API_KEY=
SEC_EDGAR_USER_AGENT=kabu-shihyo-tool your-email@example.com
EDINET_API_KEY=
